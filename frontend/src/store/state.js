import * as OPTIONS from "../data/OPTIONS.js";

export default {
  mapsApi: null,
  show_markers: false,
  show_heatmap: true,
  drawer_open: false,
  heatmap_type: OPTIONS.HEATMAP_TYPES.PROPERTY,
};
